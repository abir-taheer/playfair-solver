name: Node.js CI
on: [push]
jobs:
  test-against-examples:
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Test
        run: npm test
